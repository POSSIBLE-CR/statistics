var define = require("./../utils/constantsmanager")(exports);
 
 define({
   
    GENERAL : {
        //Error message for a 500 status code
        ERROR_MESSAGE_500 : 'Unexpected error'
    }
 
 });
